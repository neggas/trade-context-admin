import type { Styles } from "@chakra-ui/theme-tools";

const styles: Styles = {
  global: {
    body: {
      bg: "background",
      color: "text",
      fontFamily: "body",
    },
  },
};

export default styles;
